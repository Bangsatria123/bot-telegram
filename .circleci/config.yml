# This config was automatically generated from your source code
# Stacks detected: deps:node:.
version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: antasena/label
    steps:
      - run: echo "Hi I'm on a Container Runner!"
workflows:
  build-workflow:
    jobs:
      - build
    # - deploy:
    #     requires:
    #       - test-node
